#pragma once

#include <string>
#include <vector>

namespace config {

// Edit this list to control which symbols are queried.
inline const std::vector<std::string> kSymbols = {
    "AAPL", "MSFT", "NVDA"
};

}  // namespace config

